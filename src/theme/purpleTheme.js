import { createTheme } from '@mui/material/styles'

const purpleTheme = createTheme({
  palette: {
    primary: {
      main: '#262254'
    },
    secondary: {
      main: '#543884'
    }
  }
})

export default purpleTheme
